
function Image (){
    //element to take up 100% width of parent. other elements to have show over it because of higher z-index 
    return (
        <div class = "image">
            <h3>Some image in the background</h3>
        </div>
    );
}

export default Image;